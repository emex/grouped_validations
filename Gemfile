# A sample Gemfile
source "http://rubygems.org"

rails_version = ENV['RAILS_VERSION']
rails_version = rails_version ? "~> #{rails_version}" : '>= 4.0.0'

gem 'rake'
gem 'rspec', '2.14.1'
gem 'activesupport', rails_version
gem 'activemodel', rails_version
